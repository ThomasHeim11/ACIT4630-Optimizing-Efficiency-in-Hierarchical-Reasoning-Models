# SHREK-Small — ~7M parameters, competes with TRM-Att (7M).
# Narrows hidden_size from Large (512 → 384) and keeps the full hierarchical
# layer count (H=2, L=2) so the H/L hierarchy is preserved at small scale.

name: hrm.hrm_act_v1@HierarchicalReasoningModel_ACTV1
loss:
  name: losses@ACTLossHead
  loss_type: stablemax_cross_entropy

halt_exploration_prob: 0.1
halt_max_steps: 16  # same as HRM — fair comparison at equal compute

H_cycles: 2
L_cycles: 2

# SHREK: 2 layers each level — preserves the H/L hierarchy at the small scale.
H_layers: 2
L_layers: 2

hidden_size: 384   # narrowed from 512 to hit ~7M total params
num_heads: 6       # head_dim = 384/6 = 64 — same head dim as Large
expansion: 4       # same as HRM baseline — fair comparison

puzzle_emb_ndim: ${.hidden_size}

pos_encodings: rope

# SHREK: error injection magnitude follows a cosine decay across training,
# starting at alpha_max and reaching exactly zero at the final step.
alpha_max: 0.01

# SHREK: ablation flag — set to false for the HRM+EMA baseline run.
enable_error_injection: true
